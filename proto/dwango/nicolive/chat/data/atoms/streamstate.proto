syntax = "proto3";

package dwango.nicolive.chat.data.atoms;
option go_package = "github.com/genkaieng/nicolive-csp/gen/pb";

message StreamStateChange {
  int64 epoch = 1;

  enum State {
    PASSTHROUGH = 0; 
    PROCESSED = 1; 
  }

  
  State state = 2;
}
